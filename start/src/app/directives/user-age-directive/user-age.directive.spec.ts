import { UserAgeDirective } from './user-age.directive';

describe('UserAgeDirective', () => {
  it('should create an instance', () => {
    const directive = new UserAgeDirective();
    expect(directive).toBeTruthy();
  });
});
